#include "shell.h"

int execute(char **command)
{
	/* pid_t exec_command; */

	/* exec_command = fork(); */

	/* if (exec_command == 0) */
	/* 	if (execve(*command, command, NULL) == -1) */
	/* 	{ */
	/* 		return (0); */
	/* 	} */
	return (1);
}

